/* eslint-disable indent */
// eslint-disable-next-line no-undef
const User = require('./../models/User');

describe.skip('Tests about user API', () => {
    
    it('List active users ', async () => {
        expect(true).toBe(true);
    });
});